# 为什么要给模型加工具调用？

> 实测环境：`glm-4.7-flash`（智谱免费档），2026-09-07

## 一个反直觉的事实

你问「123 * 456 等于多少」，模型直接答对了。**那为什么还要给它一个计算器？**

因为**它根本没有在"算"，它是在"猜下一个字符"**。

现代推理模型确实能把简单乘法做对——但方式是在内部**写竖式**：
把 `4837 × 9271` 拆成 `4837 × 9000 + 4837 × 200 + ...`，一步一步生成中间结果，
最后拼出答案。这不是算术单元在工作，是**语言模式在模仿算术过程**。

## 实测数据（同一道题：`4837 * 9271`）

| | 不给工具 | 给计算器工具 |
|---|---|---|
| 总 token | **2020** | **217** |
| 其中推理 token | 1998 | 25 |
| 推理文本长度 | 4013 字符 | 37 字符 |
| 最终结果 | ❌ token 配额被思考吃光，**答案都没输出** | ✅ 输出 `{"expression":"4837 * 9271"}` |

**成本差 9.3 倍，而且不给工具的那一版失败了。**

## 更关键的一点：有些事它永远做不到

问「现在北京时间几点几分」：

```
真实时间  2026-09-07 21:00:55
模型回答  14:30
```

**它凭空编了一个看起来像时间的数字。** 模型没有时钟、没有随机数发生器、
访问不了你的数据库、看不到实时的股价和天气。

这些场景下它只有两条路：
1. 说「我不知道」（好模型会这样）
2. **编一个像模像样的答案**（绝大多数情况）

第二种就是**幻觉**，也是 LLM 落地最大的隐患。

## 所以工具调用解决了什么

| 问题 | 工具怎么解决 |
|---|---|
| 计算不可靠、成本高 | 交给计算器，确定性执行，1 token 都不用想 |
| 不知道当前时间/实时数据 | 交给 `get_current_time` / 搜索 API |
| 无法产生真随机 | 交给 `roll_dice` 用代码生成 |
| 碰不到私有数据 | 交给数据库查询工具 |

**核心分工：模型负责"决定做什么"，代码负责"真正去做"。**

模型从头到尾没执行过一行 Python——它只是在说「请帮我算这个」。

## 一句话总结

> LLM 的本质是**预测下一个 token 的概率模型**，不是计算器、不是时钟、不是数据库。
> 工具调用就是把「它不擅长的事」外包给确定性的代码，让它只做自己擅长的：理解意图、做决策、组织语言。

这也是为什么 ReAct（Reasoning + Acting）会成为 Agent 的地基——
**"想"和"做"必须分开**，混在一起就是又贵又不可靠。
