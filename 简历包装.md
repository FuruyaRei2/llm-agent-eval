# 这个项目怎么包装进简历

> 只写事实，不写你没做过的。所有数字都来自 `reports/` 和 `traces/`，面试前再核一遍。

---

## 一、先说结论：标题决定生死

**别把它叫「手写 ReAct Agent」。** 这个名字会让你被归到"跟着教程做了一遍"那一类。

建议的简历标题：

```
LLM Agent 评测与可观测性平台（Eval + Tracing）
技术栈：Python / OpenAI SDK / FastAPI / SSE / JSONL Trace
```

同样一个项目，标题不同，面试官的第一句话完全不同：

| 标题 | 面试官第一反应 |
|---|---|
| 手写 ReAct 智能体 | 「哦，网上教程那个，说说 tool_call 怎么拼的」→ 1 分钟结束 |
| Agent 评测平台 | 「你怎么定义好坏？指标怎么设计？」→ 能聊 20 分钟 |

---

## 二、简历正文（三到四条，直接可用）

### 版本 A：偏工程开发岗（推荐）

**LLM Agent 评测与可观测性平台**　|　个人项目　|　Python · FastAPI · OpenAI SDK

- 从零搭建 Agent 评测平台，覆盖**任务集设计 → 批量并发采样 → 多维判分 → A/B 报告**全链路；20 道任务分单步/多步/安全/健壮性四类，每题独立采样 K=3，单次全量 60 轮约 1 分钟、成本 ¥0.78。
- 设计**双指标体系**：pass@1（随机输出下的稳定性）与 pass@K（能力上限）分开统计；判分采用精确匹配、关键词、工具调用序列比对与组合规则四种策略，避免单一指标失真。
- 基于 120 轮实测、928 条结构化 trace 事件，定位三类真实问题：**条件分支执行不稳定**（某多步任务 3 次仅成功 1 次，而 pass@K=100% 掩盖了该问题）、**成本热点集中**（单轮最高 32K tokens）、**工具"失败"率 8% 中大部分为安全拦截的正确行为**，据此修正了评测口径。
- 实现流式 ReAct 执行器与 SSE 实时界面，处理 tool_call 增量碎块按 index 分桶拼接、并行调用交错、末包 usage 兼容等边界；构建 trace 埋点体系，记录延迟分位数、token 归因、缓存命中与逐轮成本。

### 版本 B：偏算法/研究方向

- 构建 LLM Agent 的**量化评测方法论**：针对非确定性输出设计重复采样协议（K=3），用 pass@1 / pass@K 双指标区分"能力不足"与"执行不稳定"，并给出两者差值对应的诊断含义。
- 通过 A/B 实验验证 prompt 改动效果：实验组 pass@1 93%→92%、tokens -9%、成本 -10%，但 p95 延迟 +21%；**总分未变而逐题此消彼长**，据此判断改动收益不明确并放弃上线。
- 发现并修正**指标与人工判断冲突**的案例：规则要求"必须调用工具"，而模型对非法参数（负数面骰子）选择拒绝调用，被判为失败——论证评测指标本身需要持续校准。

---

## 三、面试高频问题（提前想好答案）

| 问题 | 你该怎么答 | 坑 |
|---|---|---|
| 为什么不用 LangChain？ | 「想先理解底层；评测必须复用真实执行路径，套框架会让我测的不再是自己的 agent」 | 别说"框架不好" |
| 20 题是你自己出的，怎么保证不是过拟合？ | 坦白样本小（20×3），但我**只用它做同题 A/B 对比、不下绝对结论**；下一步接公开集 | 别硬说"我的题很有代表性" |
| pass@1 和 pass@K 什么区别？ | pass@1 是用户真实体验（第一次就对），pass@K 是能力上限；**两者差值 = 不稳定性**，我靠它发现了条件分支问题 | 只答定义不加洞察 = 白答 |
| 规则判分不准怎么办？ | 四种策略混用 + 我正在补 LLM-as-judge，并**用规则结果反过来评估 judge 的一致率** | 别假装规则判分没缺点 |
| 指标和人工判断冲突怎么办？ | 讲负数骰子那个案例，结论是**改指标而不是改模型** | 这是你的王牌案例，必须讲 |
| 93% 统计显著吗？ | 承认不显著；改进方向是加 Wilson 置信区间 / bootstrap，报告里显示区间而非点估计 | 心虚比答错更致命 |
| 怎么保证可复现？ | 固定任务集、固定 K、固定模型版本；trace 全量留痕可回溯 | 别提 temperature（我没设过） |

---

## 四、还差什么（按性价比排序）

| # | 补强项 | 成本 | 为什么值 |
|---|---|---|---|
| 1 | **给指标加置信区间**（Wilson / bootstrap） | 1 小时，0 花费 | 直接堵死"样本太小"这个最致命的质疑，且几乎没人做 |
| 2 | **LLM-as-judge + judge 一致率评估** | 2 小时，几毛钱 | 面试必问；顺带能说出"judge 本身也有偏差"，区分度极高 |
| 3 | **接一个公开 benchmark 子集**（τ-bench / BFCL / ToolBench 选一，50 题即可） | 半天 | "在公开集上"比"我自己出题"强一个量级 |
| 4 | **失败自动归因**（工具不存在 / 参数错 / 死循环 / 超步数 / 未如实转述） | 半天 | 从"知道错了"到"知道错在哪类"，评测平台的成熟标志 |
| 5 | **与框架对比**（同一批题跑 LangChain agent） | 半天 | 把"我不用框架"从一句辩解变成一个实验结论 |
| 6 | **README 成果化 + GitHub** | 1 小时 | 简历上放链接，没人点开就白做；要有架构图和真实报告截图 |

---

## 五、必须知道的三件事

**1. 这个项目对口「Agent 开发 / AI 应用工程岗」，对纯算法研究岗帮助有限。**
算法岗看论文、顶会、比赛；这个项目证明的是工程判断力与量化意识。两个方向的准备策略不一样，别混着投。

**2. 它不能是你唯一的项目。**
评测是"内功"，面试官还想知道你能不能做出有真实用户的东西。建议再配一个端到端应用（RAG 问答、多 agent 协作、或某个具体业务场景）。

**3. 简历上所有数字必须能对上账。**
面试官最爱问的就是"这个 93% 怎么算的"。答不上来 = 造假。`reports/baseline.md` 和 `baseline-v2.md` 里有完整明细，投之前看一遍。
